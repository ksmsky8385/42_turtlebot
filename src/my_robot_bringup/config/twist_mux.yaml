twist_mux:
  ros__parameters:
    topics:
      navigation:
        topic: /cmd_vel_nav
        timeout: 0.5
        priority: 10
      fall_follow:
        topic: /cmd_vel_fall_follow
        timeout: 0.5
        priority: 90
      joystick:
        topic: /cmd_vel_joy
        timeout: 0.5
        priority: 100
    locks:
      manual_override:
        topic: /manual_override_lock
        timeout: 0.0
        priority: 50
